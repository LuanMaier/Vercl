/** Fade de interiores e transições por imagem — mesmo overlay/tempo do menu. */
import { STAGE_FADE_MS, stageFade } from './stageFade'

export const INTERIOR_FADE_OUT_MS = STAGE_FADE_MS
export const INTERIOR_FADE_IN_MS = STAGE_FADE_MS

export const interiorFade = stageFade
